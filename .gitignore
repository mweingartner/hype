.build/
.swiftpm/
*.xcodeproj
DerivedData/
.DS_Store
build/
.claude/

# Codex agent metadata (per-machine, not source).
.codex/

# Build output produced by script/build_and_run.sh.
# The .app bundle is rebuilt on demand; only the source belongs in VCS.
dist/

# Local development code-signing certificate/private-key artifacts.
# Generated by script/build_and_run.sh when no stable local signing identity exists.
.hype-codesign/

# Python bytecode caches under the AI-training pipeline.
**/__pycache__/
*.pyc
