{
  "name": "hype-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "hype-mcp-server": "./bin/hype-mcp.js"
  },
  "scripts": {
    "start": "node ./bin/hype-mcp.js",
    "build": "tsc && mv ./bin/index.js ./bin/hype-mcp.js && chmod +x ./bin/hype-mcp.js",
    "check": "sh -c 'tmp=$(mktemp -d) || exit 1; trap \"rm -rf \\\"$tmp\\\"\" EXIT HUP INT TERM; tsc --noEmit && tsc --outDir \"$tmp\" && cmp -s \"$tmp/index.js\" ./bin/hype-mcp.js && test ! -e ./bin/index.js && test -x ./bin/hype-mcp.js'"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
